В проекте используется стиль работы main and feature branch, где рабочий код находится основной ветке main, а под процесс разработки новой части проэкта отведены соответсвующие ветки. После внесения финальных правок неосновные ветки соединяются с main через merge. 
В данном случае основная ветка main, ветки под задачи:
-
